import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Order Confirmed — breathEN' };
export default function TyLayout({ children }: { children: React.ReactNode }) { return <>{children}</>; }
